import React, { Component } from 'react';
import PhotoList from 'components/Photos/PhotoList/PhotoList';

export default class PhotoListConnected extends Component {
    render() {
        return <PhotoList />;
    }
}
